/*
    WAP to find given string is palindrome or not
*/

#include<stdio.h>
#include<string.h>
int main(void)
{
    char str1[100];
    // char str2[100];

    printf("Enter a string \n");
    gets(str1);

    int len = strlen(str1);
    int a = str1[0];
    int b = str1[len/2];

    while(a <= b)
    {
        
    }

    
    
    return 0;
}